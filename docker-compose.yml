version: "3.8"

services:
  backend:
    build: ./backend
    container_name: retrojs-backend
    ports:
      - "3001:3001"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: retrojs-frontend
    ports:
      - "80:80"
    depends_on:
      - backend